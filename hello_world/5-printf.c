#include <stdio.h>

/**
 * main - the main funnction
 * @void the main function doesn't take any parameters
 *
 * Return: the main function returns 0 if no erreors occured
 */
int main(void)
{
	printf("%s", "with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
