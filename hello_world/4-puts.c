#include <stdio.h>

/**
 * main - main function
 * @void: no parameters for the main function
 *
 * Return: return 0 if no errors
 */
int main(void)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
