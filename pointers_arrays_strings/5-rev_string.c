#include <stdio.h>
#include "main.h"
/**
 * rev_string - reverses a string
 * @s: pointeur vers charcarter or first char of string
 *
 * Return: no return
 */

int compter_nombre_char(char *s)
{
        int i, j;

        for (i = 0; s[i] != '\0'; i++)
                j = i;

        return (j);
}

void rev_string(char *s)
{
	int nombre_char = compter_nombre_char(s);
	int i;
	int j = 0;
	int k;

	for (i = nombre_char; i == 0; i --)
	{
		j++;
		s[i] = s[j];
	}

	for (k = 0; k < nombre_char; k++)
		putchar

/**

	
	for (; j > 0; j--)
	{
		printf("%c", s[j]);
		       = s[total-j];
	}
*/

}
